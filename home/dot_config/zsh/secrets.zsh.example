# Kopiere diese Datei nach  ~/.config/zsh/secrets.zsh  und trage Keys ein.
# secrets.zsh ist gitignored und wird NIE committet.
#
# Für CC brauchst du KEINEN Key (Abo-Login). Nur falls Skripte Keys brauchen:
# export ANTHROPIC_API_KEY="$(pass show anthropic/api-key)"
# export OPENAI_API_KEY="$(pass show openai/api-key)"
